name: CI-CD with SSH (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled"

# ------------------------------------------------
# Original workflow kept below for reference
# ------------------------------------------------

# env:
#   AWS_REGION: us-east-1
#   ECR_REPOSITORY: company-backend
#   ACCOUNT_ID: 689593414482

# jobs:
#   build:
#     name: Build Docker Image
#     runs-on: ubuntu-latest
#
#   test:
#     name: Test Container Build
#
#   approve:
#     name: Manual Approval
#
#   push-ecr:
#     name: Push Image to ECR
#
#   deploy:
#     name: Deploy to EC2